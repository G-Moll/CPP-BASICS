#include <iostream>
using namespace std;

int main() {
    /* EXPRESSIONS
        Group of Operators and Operands that calculates a new value
    */

    /* OPERATORS
        Characters that run an operation (specific tasks)
    */

    /* OPERANDS
        Values (Variables)
    */

    int numOne;
    int numTwo;
    int totalValue;

    cout << "Please type the first value" << endl;
    cin >> numOne;

    cout << "Please type the second value" << endl;
    cin >> numTwo;

    totalValue = numOne + numTwo;
    cout << "The final result is: " << totalValue << endl;


    return 0;
}
